import {test as plop}
import {pow} from flow2

fn add(var1, var2):
    return 56

start:
    say "start"
    if (!test) {
        say "create test"
        remember test = 1
    } else {
        say "b test = {{test}}"
        do test = test + 1
        say "a test = {{test}}"
    }

    // goto end
    goto @flow2

toto:
    do vec = [1, 2]
    say plop(1, 2)

    goto end
