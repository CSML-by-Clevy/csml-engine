import {test as plop}
import {pow} from flow2

fn add(var1, var2):
    return 56

start:
    say "hello World"
    // goto end
    goto toto

toto:
    do value = "value = {{ pow(2, 5) }}"
    say value
    hold
    say plop(1, 2)

    goto end
